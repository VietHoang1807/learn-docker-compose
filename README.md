### Tài liệu hoá thành phần cấu hình của docker compose

------------------
### Run docker compose
``shell
  docker compose -f {folder/file.yml} up -d // run
  docker compose -f {folder/file.yml} down // down
``
